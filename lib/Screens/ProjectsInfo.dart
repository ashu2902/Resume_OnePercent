import 'package:flutter/material.dart';

class ProjectsInfo extends StatelessWidget {
  const ProjectsInfo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text('Experience Info'),
      ),
    );
  }
}
